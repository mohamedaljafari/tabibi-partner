name: CI - طبيب شريك (تطبيق مقدم الخدمة)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: الفحص والفحوصات
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: تثبيت Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: تثبيت pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: تثبيت الاعتماديات
        run: pnpm install --frozen-lockfile

      - name: فحص TypeScript
        run: pnpm run check

      - name: تشغيل الفحوصات
        run: pnpm test
